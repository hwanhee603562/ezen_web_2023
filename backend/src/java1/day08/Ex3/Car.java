package java1.day08.Ex3;

// 현 클래스의 사용목적 : 설계도
public class Car {
	// 1. 필드	: 객체의 데이터를 저장하는 곳
	String model;	// 선언시 = (대입) 초기화 값이 없으므로 기본값 null
	boolean start;	// 선언시 = (대입) 초기화 값이 없으므로 기본값 false
	int speed;		// 선언시 = (대입) 초기화 값이 없으므로 기본값 0
	
	// 2. 생성자	: 객체 생성시 초기화 역할	* 클래스명과 동일하게 선언

	
	// 3. 메서드	: 객체의 동작으로 호출시 실행되는 블록
	

}
